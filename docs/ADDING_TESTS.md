# Руководство по добавлению психологических тестов

Это руководство описывает процесс добавления нового психологического теста в систему ZharqynBala.

## Содержание

1. [Обзор структуры](#обзор-структуры)
2. [Шаг 1: Определение теста](#шаг-1-определение-теста)
3. [Шаг 2: Создание вопросов](#шаг-2-создание-вопросов)
4. [Шаг 3: Добавление логики интерпретации](#шаг-3-добавление-логики-интерпретации)
5. [Шаг 4: Seed скрипт](#шаг-4-seed-скрипт)
6. [Шаг 5: Запуск и тестирование](#шаг-5-запуск-и-тестирование)
7. [Примеры](#примеры)

---

## Обзор структуры

### Файлы для редактирования

| Файл | Назначение |
|------|------------|
| `backend/prisma/test-data/psychological-tests.ts` | Данные тестов (вопросы, варианты ответов) |
| `backend/src/modules/results/scoring.service.ts` | Логика подсчёта баллов и интерпретации |
| `backend/prisma/seed.ts` | Точка входа для seed (импортирует test-data) |

### Модели базы данных

```
Test (Тест)
├── Question (Вопросы)
│   └── AnswerOption (Варианты ответов)
└── TestSession (Сессии прохождения)
    ├── Answer (Ответы пользователя)
    └── Result (Результат)
```

### Категории тестов (TestCategory)

```typescript
enum TestCategory {
  ANXIETY        // Тревожность
  MOTIVATION     // Мотивация
  ATTENTION      // Внимание
  EMOTIONS       // Эмоции
  CAREER         // Профориентация
  SELF_ESTEEM    // Самооценка
  SOCIAL         // Социальные навыки
  COGNITIVE      // Когнитивное развитие
}
```

### Типы вопросов (QuestionType)

```typescript
enum QuestionType {
  MULTIPLE_CHOICE  // Выбор одного варианта
  SCALE            // Шкала (например, от 0 до 4)
  YES_NO           // Да/Нет
  TEXT             // Текстовый ответ
}
```

---

## Шаг 1: Определение теста

Создайте объект с метаданными теста в файле `backend/prisma/test-data/psychological-tests.ts`:

```typescript
export const myNewTest = {
  id: 'test-mytest-1',  // Уникальный ID (используется для связи с интерпретатором)
  titleRu: 'Название теста на русском',
  titleKz: 'Тест атауы қазақша',
  descriptionRu: 'Описание теста и его цели.',
  descriptionKz: 'Тест сипаттамасы.',
  category: TestCategory.ANXIETY,  // Выберите категорию
  ageMin: 7,
  ageMax: 17,
  durationMinutes: 15,
  price: 0,           // 0 = бесплатный
  isPremium: false,
  order: 10,          // Порядок отображения в каталоге
};
```

---

## Шаг 2: Создание вопросов

### Тип MULTIPLE_CHOICE (выбор одного варианта)

```typescript
export const myTestQuestions = [
  {
    questionTextRu: 'Текст вопроса на русском?',
    questionTextKz: 'Сұрақ мәтіні қазақша?',
    questionType: QuestionType.MULTIPLE_CHOICE,
    options: [
      { textRu: 'Вариант 1', textKz: 'Нұсқа 1', score: 0 },
      { textRu: 'Вариант 2', textKz: 'Нұсқа 2', score: 1 },
      { textRu: 'Вариант 3', textKz: 'Нұсқа 3', score: 3 },
    ],
  },
  // ... остальные вопросы
];
```

### Тип SCALE (шкала)

```typescript
{
  questionTextRu: 'Я часто чувствую тревогу.',
  questionTextKz: 'Мен жиі үрейленемін.',
  questionType: QuestionType.SCALE,
  options: [
    { textRu: 'Никогда', textKz: 'Ешқашан', score: 0 },
    { textRu: 'Редко', textKz: 'Сирек', score: 1 },
    { textRu: 'Иногда', textKz: 'Кейде', score: 2 },
    { textRu: 'Часто', textKz: 'Жиі', score: 3 },
    { textRu: 'Очень часто', textKz: 'Өте жиі', score: 4 },
  ],
}
```

### Тип с категориями (для профориентации)

```typescript
{
  questionTextRu: 'Какая деятельность вам ближе?',
  questionTextKz: 'Сізге қай қызмет жақынырақ?',
  questionType: QuestionType.MULTIPLE_CHOICE,
  pairNumber: 1,  // Номер пары (для ДДО Климова)
  options: [
    {
      textRu: 'Ухаживать за животными',
      textKz: 'Жануарларға күтім жасау',
      score: 1,
      category: 'nature'  // Категория для подсчёта
    },
    {
      textRu: 'Ремонтировать технику',
      textKz: 'Техниканы жөндеу',
      score: 1,
      category: 'tech'
    },
  ],
}
```

---

## Шаг 3: Добавление логики интерпретации

Откройте файл `backend/src/modules/results/scoring.service.ts`.

### 3.1 Добавьте уровни интерпретации

```typescript
// В начале файла, после существующих констант
const MY_TEST_LEVELS: ScoreLevel[] = [
  {
    minScore: 0,
    maxScore: 10,
    level: 'low',
    titleRu: 'Низкий уровень',
    titleKz: 'Төмен деңгей',
    descriptionRu: 'Описание низкого уровня...',
    recommendations: [
      'Рекомендация 1',
      'Рекомендация 2',
    ],
  },
  {
    minScore: 11,
    maxScore: 20,
    level: 'average',
    titleRu: 'Средний уровень',
    titleKz: 'Орташа деңгей',
    descriptionRu: 'Описание среднего уровня...',
    recommendations: [
      'Рекомендация 1',
      'Рекомендация 2',
    ],
  },
  {
    minScore: 21,
    maxScore: 30,
    level: 'high',
    titleRu: 'Высокий уровень',
    titleKz: 'Жоғары деңгей',
    descriptionRu: 'Описание высокого уровня...',
    recommendations: [
      'Рекомендация 1',
      'Рекомендация 2',
    ],
  },
];
```

### 3.2 Добавьте метод расчёта

```typescript
// В классе ScoringService добавьте метод
private calculateMyTest(session: any): ScoringResult {
  let totalScore = 0;
  const maxScore = 30; // Максимально возможный балл

  for (const answer of session.answers) {
    if (answer.answerOption) {
      totalScore += answer.answerOption.score || 0;
    }
  }

  const level = this.findLevel(totalScore, MY_TEST_LEVELS);
  const percentage = Math.round((totalScore / maxScore) * 100);

  return {
    totalScore,
    maxScore,
    percentage,
    level: level?.level || 'unknown',
    interpretation: level?.descriptionRu || 'Интерпретация недоступна',
    recommendations: level?.recommendations || [],
  };
}
```

### 3.3 Подключите метод в calculateResult

```typescript
// В методе calculateResult() добавьте условие
async calculateResult(sessionId: string, testId: string): Promise<ScoringResult> {
  // ... существующий код ...

  const testIdLower = testId.toLowerCase();

  // Добавьте проверку для вашего теста
  if (testIdLower.includes('mytest')) {
    return this.calculateMyTest(session);
  }

  // ... остальные условия ...
}
```

---

## Шаг 4: Seed скрипт

Добавьте код создания теста в `backend/prisma/test-data/psychological-tests.ts` в функцию `seedValidatedTests`:

```typescript
export async function seedValidatedTests(prisma: PrismaClient) {
  console.log('📚 Seeding validated psychological tests...');

  // ... существующие тесты ...

  // ============================================
  // МОЙ НОВЫЙ ТЕСТ
  // ============================================
  const myTestRecord = await prisma.test.upsert({
    where: { id: myNewTest.id },
    update: {},
    create: {
      id: myNewTest.id,
      titleRu: myNewTest.titleRu,
      titleKz: myNewTest.titleKz,
      descriptionRu: myNewTest.descriptionRu,
      descriptionKz: myNewTest.descriptionKz,
      category: myNewTest.category,
      ageMin: myNewTest.ageMin,
      ageMax: myNewTest.ageMax,
      durationMinutes: myNewTest.durationMinutes,
      price: myNewTest.price,
      isPremium: myNewTest.isPremium,
      order: myNewTest.order,
    },
  });

  // Добавляем вопросы
  for (let i = 0; i < myTestQuestions.length; i++) {
    const q = myTestQuestions[i];
    const questionId = `mytest-q-${i + 1}`;

    const question = await prisma.question.upsert({
      where: { id: questionId },
      update: {},
      create: {
        id: questionId,
        testId: myTestRecord.id,
        questionTextRu: q.questionTextRu,
        questionTextKz: q.questionTextKz,
        questionType: q.questionType,
        order: i + 1,
        isRequired: true,
      },
    });

    // Добавляем варианты ответов
    for (let j = 0; j < q.options.length; j++) {
      const opt = q.options[j];
      await prisma.answerOption.upsert({
        where: { id: `mytest-q${i + 1}-opt${j + 1}` },
        update: {},
        create: {
          id: `mytest-q${i + 1}-opt${j + 1}`,
          questionId: question.id,
          optionTextRu: opt.textRu,
          optionTextKz: opt.textKz,
          score: opt.score,
          order: j + 1,
          metadata: opt.category ? { category: opt.category } : undefined,
        },
      });
    }
  }
  console.log('  ✅ Мой новый тест - X вопросов');
}
```

---

## Шаг 5: Запуск и тестирование

### Локально

```bash
cd backend

# Применить миграции (если есть изменения схемы)
npx prisma migrate dev

# Запустить seed
npx prisma db seed

# Запустить сервер
npm run start:dev
```

### На продакшене (Railway)

После пуша изменений:

```bash
# В Railway Console выполните:
npx prisma db seed
```

### Проверка

1. Откройте админку: `/admin/tests`
2. Убедитесь, что тест появился в списке
3. Пройдите тест как пользователь
4. Проверьте, что результат интерпретируется корректно

---

## Примеры

### Пример 1: Простой тест (сумма баллов)

См. **Тест школьной мотивации Лускановой**:
- `schoolMotivationTest` - метаданные
- `schoolMotivationQuestions` - вопросы
- `calculateSchoolMotivation()` - расчёт

### Пример 2: Обратная шкала

См. **Тест самооценки Ковалёва** (`calculateSelfEsteem()`):
- Чем выше балл, тем ниже самооценка
- Процент инвертируется для понимания

### Пример 3: Категории (профориентация)

См. **ДДО Климова** (`calculateCareerOrientation()`):
- Баллы считаются по 5 категориям отдельно
- Результат показывает топ-3 категории
- Используется `metadata.category` в вариантах ответов

---

## Чеклист добавления теста

- [ ] Создан объект теста с метаданными
- [ ] Созданы вопросы с вариантами ответов (RU + KZ)
- [ ] Добавлены уровни интерпретации в `scoring.service.ts`
- [ ] Добавлен метод расчёта `calculateMyTest()`
- [ ] Метод подключен в `calculateResult()`
- [ ] Добавлен код в seed функцию
- [ ] Тест запущен локально и проверен
- [ ] Изменения закоммичены и запушены
- [ ] Seed выполнен на продакшене

---

## Структура файлов

```
backend/
├── prisma/
│   ├── schema.prisma              # Модели БД
│   ├── seed.ts                    # Точка входа seed
│   └── test-data/
│       └── psychological-tests.ts # Данные тестов
└── src/
    └── modules/
        └── results/
            └── scoring.service.ts # Логика интерпретации
```

---

## Частые ошибки

1. **Тест не появляется в списке**
   - Убедитесь что `isActive: true`
   - Проверьте что seed выполнился без ошибок

2. **Результат "Интерпретация недоступна"**
   - Проверьте условие в `calculateResult()` - должен совпадать ID теста
   - Убедитесь что диапазоны баллов в уровнях покрывают все возможные значения

3. **Ошибка при seed**
   - Проверьте уникальность ID теста, вопросов и вариантов
   - Используйте `upsert` вместо `create` для идемпотентности

---

## Контакты

При возникновении вопросов обращайтесь к разработчику.
