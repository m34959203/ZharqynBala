# Railway Troubleshooting - Приложение не запускается

## Проблема
Healthcheck failed - приложение не отвечает на `/health`

## Причина
Скорее всего отсутствует DATABASE_URL или PostgreSQL база данных не создана.

## Решение

### Шаг 1: Создать PostgreSQL на Railway

1. Откройте ваш проект на Railway.app
2. Нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. Railway автоматически добавит переменную `DATABASE_URL` в ваш backend сервис

### Шаг 2: Проверить логи приложения

1. Откройте ваш backend сервис на Railway
2. Перейдите на вкладку **"Deployments"**
3. Откройте последний deployment (commit 8486d17)
4. Нажмите **"View Logs"** (НЕ Build logs, а Runtime logs!)

**Что искать в логах:**
- Ошибки подключения к БД: `Error: P1001`
- Ошибки миграций: `prisma migrate deploy failed`
- Отсутствие вывода: значит приложение упало при старте

### Шаг 3: Временно отключить миграции (для диагностики)

Измените команду запуска в Railway Variables:

**Было:**
```
RAILWAY_RUN_CMD=npm run start:migrate:prod
```

**Временно поставить:**
```
RAILWAY_RUN_CMD=npm run start:prod
```

Это запустит приложение БЕЗ миграций чтобы увидеть реальную ошибку.

### Шаг 4: Обязательные переменные окружения

В Railway Variables убедитесь что установлены:

```bash
# ОБЯЗАТЕЛЬНО
NODE_ENV=production
PORT=3001
DATABASE_URL=<автоматически от PostgreSQL>

# JWT секреты (сгенерируйте!)
JWT_SECRET=<сгенерируйте 64 символа>
JWT_REFRESH_SECRET=<сгенерируйте 64 символа>

# Опционально
CORS_ORIGIN=*
```

**Генерация секретов:**
```bash
# В терминале:
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

### Шаг 5: После настройки - Redeploy

После добавления PostgreSQL и переменных:
1. Railway автоматически сделает redeploy
2. ИЛИ вручную: **Settings** → **Redeploy**

## Ожидаемый результат

После правильной настройки в логах должно быть:
```
✅ Database connected
🚀 Application is running on: http://localhost:3001
```

И healthcheck будет успешен.

## Быстрая проверка

После успешного деплоя проверьте:
```bash
curl https://your-app.up.railway.app/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "2025-12-05T20:40:00.000Z",
  "uptime": 123.456
}
```
